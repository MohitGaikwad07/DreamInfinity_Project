import { io } from 'socket.io-client'; import { apiClient, getStoredToken } from './apiClient.js';
export const interviewRoomService = { create: (payload) => apiClient.post('/interview-room/create', payload).then(({ data }) => data), join: (payload) => apiClient.post('/interview-room/join', payload).then(({ data }) => data), schedule: (payload) => apiClient.post('/interview-room/schedule', payload).then(({ data }) => data), history: () => apiClient.get('/interview-room/history').then(({ data }) => data), socket: () => io(import.meta.env.VITE_SOCKET_URL || 'http://localhost:5000', { auth: { token: getStoredToken() }, reconnection: true }) };
