export const ReadinessGauge = ({ score = 0, breakdown = {} }) => <section className="skill-panel readiness-panel"><div className="readiness-ring" style={{ '--score': `${score * 3.6}deg` }}><div><strong>{score}</strong><span>Readiness</span></div></div><div><p className="dash-kicker">INTERVIEW READINESS</p><h3>{score >= 80 ? 'You are interview-ready' : score >= 60 ? 'You are building momentum' : 'Your plan starts here'}</h3><div className="readiness-breakdown">{Object.entries(breakdown).map(([name, value]) => <div key={name}><span>{name.replace(/([A-Z])/g, ' $1')}</span><b>{value || 0}%</b><i><em style={{ width: `${value || 0}%` }}/></i></div>)}</div></div></section>;
