const sections = [['Already learned','currentSkills','map-good'],['Strong areas','strongAreas','map-good'],['Need improvement','improvementAreas','map-warn'],['Missing skills','missingSkills','map-danger'],['Future skills','futureSkills','map-future']];
export const SkillMap = ({ assessment }) => <section className="skill-panel skill-map"><header><div><p className="dash-kicker">SKILL HEATMAP</p><h3>Skills at a glance</h3></div></header><div>{sections.map(([label,key,color]) => <article key={key}><b>{label}</b><div>{assessment?.[key]?.length ? assessment[key].map((item) => <span className={color} key={item}>{item}</span>) : <small>No data yet</small>}</div></article>)}</div></section>;
