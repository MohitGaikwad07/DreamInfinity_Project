import { useState } from 'react';
const roles = ['Frontend Developer', 'Backend Developer', 'Full Stack Developer', 'Java Developer', 'Python Developer', 'AI Engineer', 'Data Analyst', 'DevOps Engineer']; const companies = ['Google', 'Microsoft', 'Amazon', 'Adobe', 'Netflix', 'Meta', 'Oracle', 'TCS', 'Infosys', 'Wipro', 'Capgemini']; const levels = ['Fresher', '1 Year', '2 Years', '3+ Years'];
export const AssessmentForm = ({ onSubmit, loading }) => { const [targetRole, setRole] = useState(roles[0]); const [targetCompany, setCompany] = useState('Google'); const [experienceLevel, setLevel] = useState('Fresher'); return <section className="assessment-form"><div><p className="dash-kicker">PERSONALIZED ANALYSIS</p><h3>What are you aiming for?</h3></div><label>Role<select value={targetRole} onChange={(e) => setRole(e.target.value)}>{roles.map((item) => <option key={item}>{item}</option>)}</select></label><label>Company<select value={targetCompany} onChange={(e) => setCompany(e.target.value)}>{companies.map((item) => <option key={item}>{item}</option>)}</select></label><label>Experience<select value={experienceLevel} onChange={(e) => setLevel(e.target.value)}>{levels.map((item) => <option key={item}>{item}</option>)}</select></label><button disabled={loading} onClick={() => onSubmit({ targetRole, targetCompany, experienceLevel })}>{loading ? 'Analyzing your profile…' : 'Generate readiness score'}</button></section>; };
