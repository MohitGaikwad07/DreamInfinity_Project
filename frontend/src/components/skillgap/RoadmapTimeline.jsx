import { FiRefreshCw } from 'react-icons/fi';
export const RoadmapTimeline = ({ roadmap = [], onRefresh, loading }) => <section className="skill-panel roadmap-panel"><header><div><p className="dash-kicker">PERSONAL ROADMAP</p><h3>Your next five weeks</h3></div><button onClick={onRefresh} disabled={loading}><FiRefreshCw /> Refresh</button></header><div className="roadmap-timeline">{roadmap.map((item, index) => <article key={`${item.week}-${item.focus}`}><span>{index + 1}</span><div><small>{item.week} · {item.priority} priority</small><h4>{item.focus}</h4><p>{item.outcomes?.join(' · ')}</p></div></article>)}</div></section>;
