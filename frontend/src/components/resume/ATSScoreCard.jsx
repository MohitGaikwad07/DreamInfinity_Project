export const ATSScoreCard = ({ score = 0, breakdown = {} }) => <section className="resume-panel ats-panel"><div className="score-ring" style={{ '--score': `${score * 3.6}deg` }}><div><strong>{score}</strong><span>/100</span></div></div><div><p className="dash-kicker">ATS READINESS</p><h3>{score >= 80 ? 'Strong candidate profile' : score >= 60 ? 'Good foundation, room to grow' : 'Let’s strengthen your resume'}</h3><p className="ats-copy">Your score considers formatting, relevant keywords, skills, projects, experience, grammar, and achievements.</p><div className="score-breakdown">{Object.entries(breakdown).map(([name, value]) => <div key={name}><span>{name}</span><b>{value || 0}%</b><i><em style={{ width: `${value || 0}%` }}/></i></div>)}</div></div></section>;
