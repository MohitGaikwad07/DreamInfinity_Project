import { useRef, useState } from 'react';
import { FiFile, FiUploadCloud } from 'react-icons/fi';

export const roles = ['Frontend Developer', 'Backend Developer', 'Full Stack Developer', 'Java Developer', 'Python Developer', 'Data Analyst', 'AI Engineer', 'DevOps Engineer'];
export const ResumeUploadCard = ({ onUpload, uploading, progress }) => { const [file, setFile] = useState(null); const [targetRole, setTargetRole] = useState(roles[0]); const input = useRef(); const accept = (selected) => { if (selected && (selected.type === 'application/pdf' || selected.name.endsWith('.docx'))) setFile(selected); }; const submit = () => file && onUpload(file, targetRole); return <section className="resume-upload-card"><div className="resume-dropzone" onDragOver={(event) => event.preventDefault()} onDrop={(event) => { event.preventDefault(); accept(event.dataTransfer.files[0]); }} onClick={() => input.current.click()}><input ref={input} className="hidden" type="file" accept=".pdf,.docx,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document" onChange={(event) => accept(event.target.files[0])}/><span><FiUploadCloud /></span><b>{file ? file.name : 'Drop your resume here'}</b><p>{file ? `${(file.size / 1024 / 1024).toFixed(1)} MB selected` : 'PDF or DOCX · Maximum file size 8 MB'}</p></div><div className="mt-4 grid gap-3 sm:grid-cols-[1fr_auto]"><label className="text-xs text-violet-100/70">Target role<select value={targetRole} onChange={(event) => setTargetRole(event.target.value)}><>{roles.map((role) => <option key={role}>{role}</option>)}</></select></label><button className="resume-primary" disabled={!file || uploading} onClick={submit}>{uploading ? `Analyzing ${progress}%` : <><FiFile /> Upload & analyze</>}</button></div>{uploading && <div className="upload-bar"><i style={{ width: `${progress}%` }}/></div>}</section>; };
