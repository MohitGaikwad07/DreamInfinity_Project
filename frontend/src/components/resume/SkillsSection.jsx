const colors = { Programming: 'tag-purple', Frameworks: 'tag-blue', Databases: 'tag-cyan', Cloud: 'tag-pink', DevOps: 'tag-orange', Tools: 'tag-gray', 'Soft Skills': 'tag-green' };
export const SkillsSection = ({ skills = [], missingSkills = [] }) => <section className="resume-panel"><h3>Skills detected</h3><div className="skill-cloud">{skills.length ? skills.map((skill) => <span className={colors[skill.category] || 'tag-gray'} key={`${skill.name}-${skill.category}`}>{skill.name}<small>{skill.category}</small></span>) : <p className="empty-copy">Upload a resume to see your skills.</p>}</div>{missingSkills.length > 0 && <><h3 className="mt-6">Missing skills to consider</h3><div className="missing-cloud">{missingSkills.map((skill) => <span key={skill}>+ {skill}</span>)}</div></>}</section>;
