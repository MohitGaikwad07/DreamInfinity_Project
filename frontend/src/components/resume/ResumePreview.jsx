import { FiDownload, FiFileText } from 'react-icons/fi';
export const ResumePreview = ({ resume }) => <section className="resume-panel preview-panel"><header><div><p className="dash-kicker">CURRENT RESUME</p><h3>File preview</h3></div>{resume && <a href={resume.fileUrl} target="_blank" rel="noreferrer" className="preview-download"><FiDownload /> Download</a>}</header>{resume ? <div className="file-preview"><FiFileText/><b>{resume.originalFilename}</b><span>{resume.mimeType === 'application/pdf' ? 'PDF document' : 'DOCX document'} · Uploaded {new Date(resume.createdAt).toLocaleDateString()}</span><a href={resume.fileUrl} target="_blank" rel="noreferrer">Open original file →</a></div> : <div className="file-preview empty"><FiFileText/><p>Your uploaded resume will appear here.</p></div>}</section>;
