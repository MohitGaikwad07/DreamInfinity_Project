import { FiDownload, FiFileText, FiRefreshCw, FiTrash2 } from 'react-icons/fi';
export const ResumeHistory = ({ resumes, currentId, onSelect, onDelete, onAnalyze }) => <section className="resume-panel history-panel"><header><div><p className="dash-kicker">YOUR FILES</p><h3>Resume history</h3></div><span>{resumes.length} saved</span></header><div className="history-list">{resumes.length ? resumes.map((resume) => <div className={`history-item ${currentId === resume._id ? 'selected' : ''}`} key={resume._id}><button onClick={() => onSelect(resume._id)}><FiFileText/><span><b>{resume.originalFilename}</b><small>{resume.targetRole} · {new Date(resume.createdAt).toLocaleDateString()}</small></span></button><strong>{resume.atsScore}</strong><a href={resume.fileUrl} target="_blank" rel="noreferrer" title="Download"><FiDownload/></a><button title="Reanalyze" onClick={() => onAnalyze(resume._id)}><FiRefreshCw/></button><button className="delete-resume" title="Delete" onClick={() => onDelete(resume._id)}><FiTrash2/></button></div>) : <p className="empty-copy">No resumes uploaded yet.</p>}</div></section>;
