export const TranscriptPanel=({turns})=><aside className="transcript-panel"><header><p className="dash-kicker">LIVE TRANSCRIPT</p><h3>{turns.length} answered</h3></header>{turns.length?turns.map((turn,index)=><article key={`${turn.question}-${index}`}><span>Q{index+1}</span><b>{turn.question}</b><p>{turn.answer}</p></article>):<p className="empty-copy">Your questions and answers will appear here.</p>}</aside>;
