import { useState } from 'react';
import { Link } from 'react-router-dom';
import { FiMenu, FiX } from 'react-icons/fi';

const links = [['Home', '#home'], ['Features', '#features'], ['AI Assistant', '#assistant'], ['Community', '#community'], ['Pricing (Soon)', '#pricing'], ['Contact', '#contact']];
export const Navbar = () => { const [open, setOpen] = useState(false); return <header className="fixed inset-x-0 top-0 z-50 px-4 pt-4"><nav className="mx-auto flex max-w-7xl items-center justify-between rounded-2xl border border-white/10 bg-[#0d0b16]/75 px-4 py-3 backdrop-blur-xl sm:px-6"><Link className="text-lg font-extrabold tracking-tight" to="/">Dream <span className="text-violet-400">Infinity</span></Link><div className="hidden items-center gap-6 lg:flex">{links.map(([name, href]) => <a className="text-sm text-violet-100/70 hover:text-white" href={href} key={name}>{name}</a>)}</div><div className="hidden items-center gap-3 sm:flex"><Link className="text-sm font-medium text-violet-100 hover:text-white" to="/login">Login</Link><Link className="rounded-xl bg-gradient-to-r from-violet-600 to-blue-600 px-4 py-2 text-sm font-bold text-white" to="/register">Register</Link></div><button className="text-xl sm:hidden" onClick={() => setOpen(!open)} aria-label="Toggle navigation">{open ? <FiX /> : <FiMenu />}</button></nav>{open && <div className="mx-auto max-w-7xl rounded-b-2xl border border-white/10 bg-[#0d0b16]/95 p-5 backdrop-blur-xl lg:hidden">{links.map(([name, href]) => <a onClick={() => setOpen(false)} className="block py-2 text-sm text-violet-100/80" href={href} key={name}>{name}</a>)}<div className="mt-3 flex gap-3"><Link to="/login" className="text-sm text-violet-200">Login</Link><Link to="/register" className="text-sm font-bold text-violet-400">Register →</Link></div></div>}</header>; };
