import { useState } from 'react';
import { FiChevronDown } from 'react-icons/fi';
const questions = [['What is Dream & Infinity?','Dream & Infinity is an interview preparation platform that combines AI coaching, resume feedback, coding practice, mock interviews, and a community of candidates.'],['Is Dream & Infinity free to use?','You can begin with the core preparation experience for free. Additional plans will be introduced later for advanced capabilities.'],['Which interview types can I practise?','The product is designed for behavioural, technical, coding, system-design, resume, and company-specific interview preparation.'],['Does AI replace a human mock interview?','No. AI helps you practise more often and more deliberately. Human video sessions add the live interaction and perspective that matter too.']];
export const FAQSection = () => { const [active, setActive] = useState(0); return <section className="section-block max-w-3xl"><div className="section-heading"><p className="eyebrow">Questions, answered</p><h2>Everything you need to know.</h2></div><div className="mt-10 divide-y divide-white/10 rounded-2xl border border-white/10 bg-white/[.025] px-5">{questions.map(([question,answer], index) => <div key={question}><button className="flex w-full items-center justify-between gap-5 py-5 text-left font-semibold" onClick={() => setActive(active === index ? -1 : index)}>{question}<FiChevronDown className={`shrink-0 text-violet-300 transition ${active === index ? 'rotate-180' : ''}`} /></button>{active === index && <p className="pb-5 leading-7 text-violet-100/65">{answer}</p>}</div>)}</div></section>; };
