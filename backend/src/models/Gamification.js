import mongoose from 'mongoose';
const activitySchema = new mongoose.Schema({ user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, action: { type: String, required: true }, xp: { type: Number, required: true }, coins: { type: Number, default: 0 }, description: String }, { timestamps: true, versionKey: false });
const achievementSchema = new mongoose.Schema({ user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, key: { type: String, required: true }, title: String, tier: { type: String, enum: ['bronze', 'silver', 'gold', 'platinum', 'diamond', 'legend'], default: 'bronze' }, earnedAt: { type: Date, default: Date.now } }, { versionKey: false }); achievementSchema.index({ user: 1, key: 1 }, { unique: true });
const challengeSchema = new mongoose.Schema({ user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, key: String, title: String, type: { type: String, enum: ['daily', 'weekly', 'monthly'] }, target: Number, progress: { type: Number, default: 0 }, rewardXp: Number, rewardCoins: Number, endsAt: Date, completed: { type: Boolean, default: false } }, { timestamps: true, versionKey: false });
export const XPActivity = mongoose.model('XPActivity', activitySchema); export const Achievement = mongoose.model('Achievement', achievementSchema); export const Challenge = mongoose.model('Challenge', challengeSchema);
