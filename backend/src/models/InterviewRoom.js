import mongoose from 'mongoose';
const messageSchema = new mongoose.Schema({ sender: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, text: String, type: { type: String, default: 'text' }, createdAt: { type: Date, default: Date.now } }, { _id: false });
const ratingSchema = new mongoose.Schema({ from: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, to: { type: mongoose.Schema.Types.ObjectId, ref: 'User' }, communication: Number, technicalKnowledge: Number, problemSolving: Number, professionalism: Number, overallExperience: Number, comments: String }, { _id: false });
const roomSchema = new mongoose.Schema({ roomId: { type: String, unique: true, index: true }, host: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, participants: [{ type: mongoose.Schema.Types.ObjectId, ref: 'User' }], company: String, role: String, scheduledFor: Date, status: { type: String, enum: ['scheduled', 'active', 'completed'], default: 'scheduled' }, messages: { type: [messageSchema], default: [] }, notes: { type: String, default: '' }, whiteboard: { type: [mongoose.Schema.Types.Mixed], default: [] }, ratings: { type: [ratingSchema], default: [] }, recordingUrl: String, recordingDuration: Number, startedAt: Date, endedAt: Date }, { timestamps: true, versionKey: false });
roomSchema.index({ participants: 1, scheduledFor: 1 }); export const InterviewRoom = mongoose.model('InterviewRoom', roomSchema);
